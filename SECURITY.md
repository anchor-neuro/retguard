# Security

This project has no server components; the attack surface is the model files
and image parsing, plus the weights download-and-verification path in
`retguard/weights.py`. Report vulnerabilities privately to
ghoneim2012@gmail.com — do not open public issues for security reports. You
will receive an acknowledgment within 7 days.
